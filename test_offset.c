#include "types.h"
#include "stat.h"
#include "user.h"

#define NUM_THREAD 10;

int write_thread(void);

int main(int argc, char *argv) {
  int i, ret;

  for(i = 0; i < NUM_THREAD; i++) {
    
  }
}
